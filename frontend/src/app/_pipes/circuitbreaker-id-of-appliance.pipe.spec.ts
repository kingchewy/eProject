import { CircuitbreakerIdOfAppliancePipe } from './circuitbreaker-id-of-appliance.pipe';

describe('CircuitbreakerIdOfAppliancePipe', () => {
  it('create an instance', () => {
    const pipe = new CircuitbreakerIdOfAppliancePipe();
    expect(pipe).toBeTruthy();
  });
});
